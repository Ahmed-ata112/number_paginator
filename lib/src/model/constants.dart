enum ContentDisplayMode {
  hidden,
  numbers,
  dropdown,
}

enum DotsVisibility {
  //will show front Dots Only
  frontOnly,
  //will show back Dots Only
  backOnly,
  //will show front and back Dots
  bothDots,
  //will show none of the Dots
  none
}
